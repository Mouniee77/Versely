<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp1"
        Title="Emotions" Height="450" Width="800">
    <Grid Margin="12">
        <!-- Removed outer ScrollViewer so items can wrap inside the ListBox's panel -->
        <ListBox ItemsSource="{Binding Emotions}"
                 BorderThickness="0"
                 Background="Transparent"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                 ScrollViewer.CanContentScroll="False">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- WrapPanel will flow items left-to-right and wrap to new rows,
                         ensuring no horizontal scrollbar and making more items visible. -->
                    <WrapPanel IsItemsHost="True" Orientation="Horizontal" VerticalAlignment="Top"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!-- Use MinWidth/MinHeight instead of fixed Width/Height so items can shrink
                         and wrap to additional rows when needed. -->
                    <Button Click="EmotionButton_Click"
                            Tag="{Binding}"
                            Margin="8"
                            MinWidth="120"
                            MinHeight="90"
                            Padding="8">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"
                                       TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding Verses.Count, StringFormat='Verses: {0}'}"
                                       FontSize="11"
                                       Foreground="Gray"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>